// Compatibility entry so create-react-app / react-scripts can find the expected `src/index.jsx` entry.
import './main.jsx';

// Note: the real render logic lives in src/main.jsx which calls createRoot.
